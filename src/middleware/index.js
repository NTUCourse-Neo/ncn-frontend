// define all csutom middlewares function here

// template
// function MIDDLEWARE_NAME({ getState, dispatch }) {
//     return (next) => (action) => {
//       // check action type you want to handle
//       // do your stuff...
//       // call next to pass action to next middleware
//       return next(action);
//     }
// }