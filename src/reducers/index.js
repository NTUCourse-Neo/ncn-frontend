// store initial states and reducers

const initState = {}

const reducer = (state = initState, action) => {
    switch (action.type){
        default:
            return state 
    }
}

export default reducer